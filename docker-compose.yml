services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nura-frontend
    ports:
      - "4001:4200"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    stdin_open: true
    tty: true
